# Environment variables for React production build

# API URL used by the frontend to make backend requests
# This will be injected into the React app at build time
REACT_APP_API_URL=https://metaverse-social-backend-prod.azurewebsites.net/api

# Environment name used for conditional behavior
REACT_APP_ENVIRONMENT=production

# Disable generation of source maps in production for smaller bundles and better security
GENERATE_SOURCEMAP=false
